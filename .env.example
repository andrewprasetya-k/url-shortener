# Used by docker-compose.yaml as a build arg for the frontend image.
# Must be the backend's PUBLICLY reachable URL (the browser calls it directly),
# not the "backend" Docker service name. Copy this to ".env" and fill in your
# real domain/port before running `docker compose build`.
NEXT_PUBLIC_API_URL=https://api.wiyata.me
